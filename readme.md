switch terminal to cmd (Command Prompt)
set up virtusl environment
py -m venv env
activate virtual env
```
env\Scripts\activate.bat
```

cd into inmest folder
```
cd inmest
```

run server 
```
py manage.py runserver

```
